import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FA8072',
      light: '#F7E7CE'
    },
    secondary: {
      main: '#088FFA',
    },
  },
});

export default theme;